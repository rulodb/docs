---
title: ge (>=)
description: 'Check if two values are greater than or equal to each other.'
---

import DocumentationFooter from '/snippets/documentation-footer.mdx';

To check if two values are greater than or equal to each other, use the `ge` function, which returns `true` if the first value is greater than or equal to the second value and `false` otherwise.

## Syntax

<CodeGroup>

```ts TypeScript
value.ge(otherValue) → result
```

</CodeGroup>

### Arguments

List of arguments to provide.

<ParamField body="otherValue" type="string | number" required>
  The value to compare with. The type of the value being compared depends on the value used for
  comparison.
</ParamField>

### Optargs

No optional arguments.

### Returns

<ParamField body="result" type="boolean">
  Returns `true` if the first value is greater than or equal to the second value and `false`
  otherwise.
</ParamField>

## Behavior

No special behavior.

## Notes & Caveats

No notes.

## Example

### Filter users that are older than 21

Filter for users in the `users` table where the user's `age` is greater than or equal to `21`.

<CodeGroup>

```ts TypeScript
await r
  .db()
  .table('users')
  .filter((doc) => doc.age.ge(21))
  .run(client);
```

</CodeGroup>

## Related Functions

- [`eq`](/sdk/logical-eq-operation) - Logical Equality Operation
- [`ne`](/sdk/logical-ne-operation) - Logical Inequality Operation
- [`lt`](/sdk/logical-lt-operation) - Logical Less Than Operation
- [`le`](/sdk/logical-le-operation) - Logical Less Than or Equal Operation
- [`gt`](/sdk/logical-gt-operation) - Logical Greater Than Operation
- [`and`](/sdk/logical-and-operation) - Logical AND Operation
- [`or`](/sdk/logical-or-operation) - Logical OR Operation
- [`not`](/sdk/logical-not-operation) - Logical NOT Operation
- [`field`](/sdk/referencing-a-field) - Referencing a Field
- [`table`](/sdk/referencing-a-table) - Referencing a Table
- [`filter`](/sdk/filter-documents) - Filtering Documents

<DocumentationFooter />
