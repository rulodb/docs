---
title: ne (!=)
description: 'Check if two values are not equal.'
---

import DocumentationFooter from '/snippets/documentation-footer.mdx';

To check if two values are not equal, use the `ne` function, which returns `true` if the two values are not equal and `false` otherwise.

## Syntax

<CodeGroup>

```ts TypeScript
value.ne(otherValue) → result
```

</CodeGroup>

### Arguments

List of arguments to provide.

<ParamField body="otherValue" type="string | number" required>
  The value to compare with. The type of the value being compared depends on the value used for
  comparison.
</ParamField>

### Optargs

No optional arguments.

### Returns

<ParamField body="result" type="boolean">
  Returns `true` if the two values are not equal and `false` otherwise.
</ParamField>

## Behavior

No special behavior.

## Notes & Caveats

No notes.

## Example

### Return users whose firstname is not "John"

Filter for users in the `users` table where the user's `first_name` is not `John`.

<CodeGroup>

```ts TypeScript
await r
  .db()
  .table('users')
  .filter((doc) => doc.first_name.ne('John'))
  .run(client);
```

</CodeGroup>

### Filter orders outside of Europe

Filter for orders in the `orders` table where the order's shipping address `continent` is not `Europe`, where the continent is a nested field of `address`.

<CodeGroup>

```ts TypeScript
await r
  .db()
  .table('orders')
  .filter((doc) => doc.field('address.continent').ne('Europe'))
  .run(client);
```

</CodeGroup>

## Related Functions

- [`eq`](/sdk/logical-eq-operation) - Logical Equality Operation
- [`lt`](/sdk/logical-lt-operation) - Logical Less Than Operation
- [`le`](/sdk/logical-le-operation) - Logical Less Than or Equal Operation
- [`gt`](/sdk/logical-gt-operation) - Logical Greater Than Operation
- [`ge`](/sdk/logical-ge-operation) - Logical Greater Than or Equal Operation
- [`and`](/sdk/logical-and-operation) - Logical AND Operation
- [`or`](/sdk/logical-or-operation) - Logical OR Operation
- [`not`](/sdk/logical-not-operation) - Logical NOT Operation
- [`field`](/sdk/referencing-a-field) - Referencing a Field
- [`table`](/sdk/referencing-a-table) - Referencing a Table
- [`filter`](/sdk/filter-documents) - Filtering Documents

<DocumentationFooter />
