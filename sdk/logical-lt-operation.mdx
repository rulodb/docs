---
title: lt (<)
description: 'Check if two values are less than each other.'
---

import DocumentationFooter from '/snippets/documentation-footer.mdx';

To check if two values are less than each other, use the `lt` function, which returns `true` if the first value is less than the second value and `false` otherwise.

## Syntax

<CodeGroup>

```ts TypeScript
value.lt(otherValue) → result
```

</CodeGroup>

### Arguments

List of arguments to provide.

<ParamField body="otherValue" type="string | number" required>
  The value to compare with. The type of the value being compared depends on the value used for
  comparison.
</ParamField>

### Optargs

No optional arguments.

### Returns

<ParamField body="result" type="boolean">
  Returns `true` if the first value is less than the second value and `false` otherwise.
</ParamField>

## Behavior

No special behavior.

## Notes & Caveats

No notes.

## Example

### Filter users that are younger than 21

Filter for users in the `users` table where the user's `age` is under `21`.

<CodeGroup>

```ts TypeScript
await r
  .db()
  .table('users')
  .filter((doc) => doc.age.lt(21))
  .run(client);
```

</CodeGroup>

### Filter low value orders

Filter for orders in the `orders` table where the order's `value` is less than `100`.

<CodeGroup>

```ts TypeScript
await r
  .db()
  .table('orders')
  .filter((doc) => doc.value.lt(100))
  .run(client);
```

</CodeGroup>

## Related Functions

- [`eq`](/sdk/logical-eq-operation) - Logical Equality Operation
- [`ne`](/sdk/logical-ne-operation) - Logical Inequality Operation
- [`le`](/sdk/logical-le-operation) - Logical Less Than or Equal Operation
- [`gt`](/sdk/logical-gt-operation) - Logical Greater Than Operation
- [`ge`](/sdk/logical-ge-operation) - Logical Greater Than or Equal Operation
- [`and`](/sdk/logical-and-operation) - Logical AND Operation
- [`or`](/sdk/logical-or-operation) - Logical OR Operation
- [`not`](/sdk/logical-not-operation) - Logical NOT Operation
- [`field`](/sdk/referencing-a-field) - Referencing a Field
- [`table`](/sdk/referencing-a-table) - Referencing a Table
- [`filter`](/sdk/filter-documents) - Filtering Documents

<DocumentationFooter />
