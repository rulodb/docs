---
title: and (&&)
description: 'Compare two values for logical AND operation.'
---

import DocumentationFooter from '/snippets/documentation-footer.mdx';

To compare two values for logical AND operation, use the `and` function, which returns `true` if both values are `true` and `false` otherwise.

## Syntax

<CodeGroup>

```ts TypeScript
value.and(otherValue) → result
```

</CodeGroup>

### Arguments

List of arguments to provide.

<ParamField body="otherValue" type="boolean" required>
  The value to compare with.
</ParamField>

### Optargs

No optional arguments.

### Returns

<ParamField body="result" type="boolean">
  Returns `true` if both values are `true` and `false` otherwise.
</ParamField>

## Behavior

No special behavior.

## Notes & Caveats

No notes.

## Example

### Filter adulst in the USA

Filter for users in the `users` table where the user's `age` is greater than or equal to `21` and the user's `country` is `USA`.

<CodeGroup>

```ts TypeScript
await r
  .db()
  .table('users')
  .filter((doc) => doc.age.ge(21).and(doc.country.eq('USA')))
  .run(client);
```

</CodeGroup>

## Related Functions

- [`eq`](/sdk/logical-eq-operation) - Logical Equality Operation
- [`ne`](/sdk/logical-ne-operation) - Logical Inequality Operation
- [`lt`](/sdk/logical-lt-operation) - Logical Less Than Operation
- [`le`](/sdk/logical-le-operation) - Logical Less Than or Equal Operation
- [`gt`](/sdk/logical-gt-operation) - Logical Greater Than Operation
- [`ge`](/sdk/logical-ge-operation) - Logical Greater Than or Equal Operation
- [`or`](/sdk/logical-or-operation) - Logical OR Operation
- [`not`](/sdk/logical-not-operation) - Logical NOT Operation
- [`field`](/sdk/referencing-a-field) - Referencing a Field
- [`table`](/sdk/referencing-a-table) - Referencing a Table
- [`filter`](/sdk/filter-documents) - Filtering Documents

<DocumentationFooter />
